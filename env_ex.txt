# -----------------------------------------------------------------------------
# AI 모델 및 API 키 설정
# -----------------------------------------------------------------------------
# 사용할 LLM 제공자를 선택합니다. ("gemini" 또는 "openai")
LLM_PROVIDER=gemini

# 텍스트 기반 추론에 사용할 LLM 모델 이름을 지정합니다.
# Gemini: "gemini-1.5-flash", "gemini-1.5-pro"
# OpenAI: "gpt-4o", "gpt-4o-mini"
LLM_MODEL_NAME=gemini-1.5-flash

# 멀티모달 검증에 사용할 모델 이름을 지정합니다.
MULTIMODAL_MODEL_NAME=gemini-1.5-flash

# Google Gemini API 키를 입력하세요.
GEMINI_API_KEY=

# OpenAI API 키를 입력하세요. (LLM_PROVIDER가 "openai"일 경우 필요)
OPENAI_API_KEY=

# -----------------------------------------------------------------------------
# 모델 경로 및 타입 설정
# -----------------------------------------------------------------------------
# 파인튜닝된 YOLO 모델 파일의 경로를 지정합니다.
YOLO_MODEL_PATH=yolo_food.pt

# 깊이 추정에 사용할 MiDaS 모델 타입을 선택합니다.
# 고정밀: "DPT_Large" (기본값)
# 중간: "DPT_Hybrid"
# 저사양: "MiDaS_small"
MIDAS_MODEL_TYPE=DPT_Large

# MiDaS 모델의 입력 이미지 크기를 지정합니다. (DPT_Large/Hybrid 권장: 384)
MIDAS_INPUT_SIZE=384

# -----------------------------------------------------------------------------
# 파이프라인 동작 설정
# -----------------------------------------------------------------------------
# 멀티모달 LLM을 이용한 시각적 검증을 활성화할지 여부를 설정합니다. (true/false)
ENABLE_MULTIMODAL=true

# 처리 결과를 'results' 폴더에 저장할지 여부를 설정합니다. (true/false)
SAVE_RESULTS=true

# 디버그 모드를 활성화하여 모든 중간 과정과 로그를 출력할지 설정합니다. (true/false)
DEBUG_MODE=false

# -----------------------------------------------------------------------------
# 기본값 설정 (필요시 수정)
# -----------------------------------------------------------------------------
# 기본 음식 밀도 (g/cm³)
DEFAULT_FOOD_DENSITY=0.7

# 일반적인 음식 포션 크기 (g)
DEFAULT_TYPICAL_PORTION=100

# 1cm에 해당하는 기본 픽셀 수 (기준 물체 없을 때 사용)
DEFAULT_PIXEL_PER_CM=100.0

# 기본 깊이 변화량
DEFAULT_DEPTH_VARIATION=1.0

# 최소 추정 신뢰도
DEFAULT_CONFIDENCE=0.3

# 추정 실패 시 기본 질량 (g)
DEFAULT_MASS=100.0